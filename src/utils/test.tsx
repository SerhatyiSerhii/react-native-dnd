// This is test. Delete in future if not needed!

export const test = () => {
  // const test = function (arr: (configType | componentType)[], targetArr: any[]): any[] {
  //   let result = targetArr;
  //   for (let i = 0; i < arr.length; i++) {
  //     if (arr[i].id === targetElem.id) {
  //       result.push(arr[i]);
  //       break;
  //     }
  //     if ((arr[i] as configType).content) {
  //       result.push(arr[i]);
  //       let x = test((arr[i] as configType).content, []);
  //       if (x.length) {
  //         result = result.concat(x);
  //         break;
  //       } else {
  //         result.pop();
  //       }
  //     }
  //   }
  //   return result;
  // }
  // const testConfig = JSON.parse(JSON.stringify(config));
  // console.log(test(testConfig, []));
};
